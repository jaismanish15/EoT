package src.test;

import org.junit.jupiter.api.Test;
import src.main.Game;
import src.main.Tournament;
import src.main.player.*;

import java.util.ArrayList;
import java.util.List;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class TournamentTest {
    @Test
    void conductTournament() {
//        List<Player> players = new ArrayList<>();
//
//       players.forEach(player -> {
//           players.forEach(anotherPlayer -> {
//               if(!player.equals(anotherPlayer))
//               new Game(player, anotherPlayer);
//           });
//       });
//
//
//
//        Tournament tournament = new Tournament(players);
//        tournament.conductTournament();
//
//        assertEquals(5, tournament.getRemainingPlayers().size());
    }
}
